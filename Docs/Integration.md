# Integration

Install the package:

- NuGet: `Kapusch.StoreKit2.iOS`

By default, the package injects its native wrapper through `buildTransitive` on iOS targets.

The managed API surface is intentionally small and designed for app-level orchestration.

For source-mode usage, see `Docs/SourceMode.md`.
